# 🤖 AI SHOWCASE — Hành trình build MemeQuant AI cùng AI

> **Hackathon:** Cook Your MVP 2025
> **Builder:** Jake
> **AI Tools:** Claude Chat (Opus 4.6) + Claude Code
> **Thời gian:** 5 ngày
> **Sản phẩm:** MemeQuant AI — Solana memecoin trading agent với risk management

---

## Cách đọc tài liệu này

Mỗi section là 1 bước trong quá trình build sản phẩm. Với mỗi bước tôi ghi lại:

- **Tôi làm gì** — quyết định, prompt, câu hỏi
- **AI làm gì** — output, phản biện, đề xuất
- **Tôi học được gì** — kiến thức mới, cách tư duy mới
- **Tiết kiệm bao lâu** — so với tự làm không có AI

**Lưu ý quan trọng:** Tôi là dân non-tech, chưa từng code trước đây. Toàn bộ product được build bằng cách: tôi mô tả ý tưởng → AI viết code/tạo prompt → tôi paste vào Claude Code → test → feedback → lặp lại.

---

## 1. Từ ý tưởng mơ hồ → Spec hoàn chỉnh

![Spec Creation](screenshots/01-spec.png)

**Tôi làm gì:** Tôi tham gia hackathon với ý tưởng "build 1 nền tảng onchain trading terminal để giúp user tự deploy AI trade memecoin". Tôi gửi Claude 2 docs: "About MemeQuant AI" + Guidebook cuộc thi, rồi nói:

> *"Hey Claude, t đang tham gia 1 cuộc thi ở công ty về build 1 sản phẩm AI, t đã quyết định build 1 nền tảng onchain trading terminal để giúp user tự deploy AI để trade memecoin/onchain tokens và tạo các automations. Có gì hãy đọc kỹ info ở trong docs và đọc kỹ Guidebook mà t đã gửi nhé. Sau đó hãy viết giúp t 1 bản Spec hoàn chỉnh."*

**AI làm gì:** Claude không chỉ viết spec mà còn bổ sung những phần tôi không nghĩ tới:
- Risk Disclosure section (bắt buộc cho trading product)
- Competitive moat analysis vs Banana Gun, Photon, BonkBot, GMGN
- Roadmap chia phase rõ ràng (MVP → v2 → v3)
- Business model với unit economics cụ thể

Sau khi nhận feedback từ BGK chỉ ra 4 điểm yếu (scope quá rộng, thiếu technical depth, thiếu honest assessment), Claude tự revise qua 4 versions:
- v1→v2: Thêm competitive landscape
- v2→v3: Cut scope từ multi-chain xuống **Solana-only**
- v3→v4: Thêm formulas, API specs, và phần "Honest Assessment" thừa nhận limitations

**Tôi học được gì:**
- Cách viết spec: Problem → Solution → Features → Biz Model → Risk
- **Scope nhỏ > Scope to:** 1 chain chạy thật > 4 chains chạy giả
- **Honest marketing > Hype marketing:** BGK đánh giá cao khi thừa nhận giới hạn
- Cho AI xem examples + guidebook = output chất lượng hơn rất nhiều

🕐 **Tiết kiệm:** ~2-3 ngày research + viết spec thủ công

---

## 2. Dân non-tech thiết kế UI — chỉ bằng mô tả tiếng Việt

![UI Design](screenshots/02-ui.png)

**Tôi làm gì:** Tôi không biết code, nhưng tôi biết mình muốn UI trông như thế nào. Tôi gửi Claude 2 ảnh layout tham khảo (từ Ethy + GMGN) và mô tả bằng tiếng Việt:

> *"Đây t gửi cho m 1 số layout để tham khảo nhé, t muốn cột bên trái là side bar, trong side bar sẽ có các button sau: Market, Portfolio, Automations, Referral, Pricing, Protocol Stats nhé. Ở giữa màn hình, chiếm diện tích nhiều nhất sẽ là UI của từng tính năng. Ở bên phải màn hình sẽ là chat terminal để user chat với agent."*

Claude hiểu ngay vision và vẽ layout diagram:
```
| SIDEBAR  |   MAIN CONTENT    | CHAT TERMINAL |
| [M] Logo |  (thay đổi tab)   | Agent logs    |
| Market   |  Market → tokens  | Stream here   |
| Portfolio|  Portfolio → holdings              |
```

**AI làm gì:** Claude Chat tạo prompts chi tiết → tôi paste vào Claude Code → Claude Code build:
- Next.js app với Tailwind CSS, dark theme
- 6 tabs navigation với sidebar collapse được
- Chat terminal panel bên phải
- Agent deploy flow (đặt tên → tạo ví → online)
- Token detail view với chart embed từ DexScreener
- Responsive layout chuyên nghiệp

Mỗi lần build xong, tôi reload localhost, nhìn kết quả, feedback chi tiết từng pixel:
> *"Background tối hơn, logo lấy từ CoinGecko, deposit address hiện copy button..."*

**Tôi học được gì:**
- **Visual reference > mô tả dài:** Gửi 2 ảnh tham khảo tốt hơn viết 500 chữ
- **Feedback loop nhanh = chất lượng:** Mô tả → Build → Nhìn → Sửa → Lặp lại
- **Tách task nhỏ:** 1 prompt = 1 feature. Không gộp nhiều thứ vào 1 prompt
- Non-tech hoàn toàn có thể "thiết kế" product — chỉ cần biết mô tả rõ mình muốn gì

🕐 **Tiết kiệm:** ~2-3 tuần thuê designer + developer

---

## 3. Build backend từ zero — được hướng dẫn từng bước

![Backend Build](screenshots/03-backend.png)

**Tôi làm gì:** Sau khi có UI, tôi cần backend thật. Tôi nói thẳng:

> *"Giờ cứ coi như là t không biết làm gì cả, m sẽ hướng dẫn t từng bước từ lúc bắt đầu code BE, merge nó vào FE để có MVP hoàn chỉnh, và push code lên github nhé."*

Kèm theo danh sách features mà MVP cần có:
- ✅ Deploy a personal agent wallet on Solana
- ✅ Chat-to-trade: parse natural language → execute buy/sell via Jupiter
- ✅ Token safety check via GoPlus API
- ✅ Market data from DexScreener

**AI làm gì:** Claude hướng dẫn tôi step-by-step, mỗi bước là 1 prompt paste vào Claude Code:

**Bước 1:** Tạo `/server` folder + package.json + dependencies (`@solana/web3.js`, `@anthropic-ai/sdk`, `bs58`)

**Bước 2:** API routes — `/api/wallet-balance`, `/api/swap`, `/api/transfer`, `/api/chat`, `/api/dexscreener`, `/api/token-security`, `/api/market-tokens`

**Bước 3:** Wallet generation — `Keypair.generate()` → save to `wallets.json`

**Bước 4:** Jupiter DEX integration — quote + swap via `api.jup.ag/swap/v1/`

**Bước 5:** GoPlus safety scoring — check honeypot, mintable, freezable, holder concentration

**Bước 6:** Anthropic Tool Use — cho Claude agent tools thật (execute_swap, execute_transfer, search_token, check_balance)

**Tôi học được gì:**
- **Blockchain = public database:** Ai cũng đọc được, chỉ cần wallet address
- **Keypair = danh tính:** Public key = địa chỉ, Secret key = mật khẩu
- **API = cách các service nói chuyện với nhau:** Frontend → Backend → Jupiter/GoPlus/DexScreener
- **Environment variables:** Tách API keys ra khỏi code để bảo mật
- **Dependency order matters:** Build wallet trước, rồi mới trade, vì trade cần wallet

🕐 **Tiết kiệm:** ~3-4 tuần học Solana dev + API integration + backend architecture

---

## 4. Debug lớn nhất — AI bịa data và bịa cả hành động

![Bug Fixing](screenshots/04-bugfix.png)

**Tôi làm gì:** Đây là phần khó nhất và nhiều drama nhất. Agent chat trả lời "đẹp" nhưng toàn bộ là giả:
- Hỏi balance → agent bịa số "3.47 SOL" (thực tế có 0.12 SOL)
- Mua token → hiện "TRADE SUCCESS" với tx hash → bấm Solscan → không tồn tại
- Transfer SOL → hiện "Transfer Complete" → tiền không chuyển

Tôi chụp screenshot gửi Claude Chat:

> *"Ê m ơi, agent đã phản hồi thông minh rồi nhé, tuy nhiên có vấn đề này: địa chỉ ví nó tạo cho t là ANJ19Deg... và t vừa mới nạp vào đó 0.12 SOL, chưa có mua bán gì cả, vậy tại sao nó check balance của t lại ra những thứ này?"*

**AI làm gì:** Claude chẩn đoán qua nhiều vòng:

**Vòng 1 — Phát hiện hallucination:** Agent đang "bịa data" vì system prompt bảo nó "respond AS IF you can execute trades" → nó tự nghĩ ra số liệu thay vì đọc blockchain thật.

**Vòng 2 — Fix sai API:** Jupiter API v6 đã deprecated → migrate sang v1. DexScreener trả về sai token mint → fix extraction logic.

**Vòng 3 — Phát hiện gốc rễ:** Claude AI agent KHÔNG BAO GIỜ gọi API thật. Nó viết `<function_calls>` XML tags giả trong chat text — đóng kịch gọi tool nhưng thực ra chỉ viết text!

**Fix cuối cùng:** Implement Anthropic **Tool Use protocol** — cho agent tools thật qua API. Khi user confirm trade, agent THỰC SỰ gọi Jupiter, ký transaction bằng keypair, gửi lên Solana blockchain, trả về tx hash thật có thể verify trên Solscan.

**Tôi học được gì:**
- **AI có thể hallucinate hành động:** Không chỉ bịa data, AI còn bịa cả việc nó đã thực hiện action
- **Tool Use > Text simulation:** Cho AI tools thật để gọi → kết quả thật. Không bao giờ để AI tự viết giả
- **Console.log = vũ khí debug:** Log từng step (STEP 1→6), biết ngay chỗ nào fail
- **Kiên nhẫn:** Mất ~8 tiếng debug liên tục, nhưng kết quả là pipeline trade thật 100%

🕐 **Tiết kiệm:** Không tiết kiệm thời gian ở bước này — nhưng **không có AI thì tôi không thể build được** trading pipeline. Đây là kiến thức level senior blockchain developer.

---

## 5. Test trên localhost — Trading agent chạy thật trên Solana mainnet

![Localhost Testing](screenshots/05-localhost.png)

**Tôi làm gì:** Sau khi fix xong tất cả bugs, tôi test từng tính năng trên localhost:

1. **Deploy agent** → tạo ví Solana thật → copy address → nạp SOL từ ví cá nhân
2. **Chat "buy $3 of BONK"** → agent search token → GoPlus safety check (0/100 SAFE) → Jupiter quote → confirm → **TRADE THẬT trên blockchain** → tx hash verify được trên Solscan ✅
3. **Chat "transfer BONK to [address]"** → agent transfer token thật → confirm trên Solscan ✅
4. **Market tab** → 4 sub-tabs (Trending, Top, Gainers, New Pairs) → click token → chart + data real-time từ DexScreener
5. **Portfolio** → hiện Total Value = SOL + tất cả token holdings tính bằng USD
6. **Agent Activity** → log đầy đủ "Bought $3.00 of BONK via chat command"

Mỗi lần tìm bug UI nhỏ (thiếu tên token, giá hiện sai format), tôi feedback cho Claude Chat → Claude tạo prompt → paste Claude Code → fix → reload.

**AI làm gì:** Claude giúp tôi fix hàng chục bug UI/UX nhỏ:
- Token name thiếu trong Recent Trades → fix
- Price format: `$0.000006` → `$0.0₅6053` (subscript notation) → fix
- Agent Activity không log transfers → fix
- Total Value chỉ tính SOL, không tính token holdings → fix (thêm Jupiter Price API)

**Tôi học được gì:**
- **Test từng feature một:** Không test hết 1 lúc, test xong 1 cái mới qua cái tiếp
- **Bug nhỏ ảnh hưởng lớn:** Thiếu tên token trong table nhìn rất unprofessional
- **Subscript price notation** ($0.0₅6053) là chuẩn industry cho memecoin — detail nhỏ nhưng BGK sẽ notice
- **Iterate nhanh:** Feedback → Fix → Test → Feedback. Mỗi vòng chỉ mất 5-10 phút

🕐 **Tiết kiệm:** ~1-2 tuần QA testing + UI polish nếu làm truyền thống

---

## 6. Deploy lên Railway — Từ localhost đến link gửi BGK

![Railway Deploy](screenshots/06-railway.png)

**Tôi làm gì:** Code chạy ngon trên localhost, giờ cần public link. Tôi hỏi:

> *"Bây giờ muốn đẩy tất cả những gì đang có trên localhost lên public server thì làm sao?"*

Claude đề xuất Railway (thay vì Vercel) vì Railway giữ file system — quan trọng cho `wallets.json`.

**AI làm gì:** Hướng dẫn tôi từng bước:
1. **Push GitHub:** `git add . → git commit → git push` (3 lệnh, tôi chưa bao giờ dùng git trước đây)
2. **Tạo Railway project:** Login with GitHub → chọn repo → auto-detect Next.js
3. **Set environment variables:** ANTHROPIC_API_KEY, JUPITER_API_KEY, SOLANA_RPC_URL
4. **Generate domain:** `memequantai-production.up.railway.app`
5. **Auto-deploy:** Mỗi lần push code mới lên GitHub → Railway tự build + deploy

Kết quả: **"Deployment successful"** ✅ — App chạy trên public URL, BGK có thể truy cập và test real trading.

Sau khi deploy, tôi tiếp tục fix bugs → push GitHub → Railway auto-redeploy. Workflow cực kỳ smooth.

**Tôi học được gì:**
- **Git cơ bản:** `add → commit → push`, 3 lệnh là đủ cho 1 non-tech developer
- **Environment variables:** Cách tách API keys ra khỏi code (bảo mật — không bao giờ push keys lên GitHub)
- **CI/CD tự động:** Push code → server tự update. Không cần SSH, không cần manual deploy
- **Railway vs Vercel:** Vercel = serverless (mất file), Railway = persistent server (giữ file)

🕐 **Tiết kiệm:** ~1-2 ngày setup DevOps + server configuration

---

## Tổng kết

### Những gì 1 người non-tech đã build trong 5 ngày:

| Feature | Status |
|---------|--------|
| Professional trading UI (6 tabs, dark theme) | ✅ |
| AI Agent chat thông minh (Anthropic Claude API) | ✅ |
| Real Solana wallet generation & persistence | ✅ |
| Real blockchain data (balance, prices, holdings) | ✅ |
| Token swap via Jupiter DEX (real on-chain) | ✅ |
| SOL & token transfer (real on-chain) | ✅ |
| GoPlus scam detection (0-100 safety score) | ✅ |
| DexScreener market data + charts | ✅ |
| 7 pre-built automation playbooks | ✅ |
| Deploy trên Railway (public URL) | ✅ |

### AI không chỉ viết code — AI dạy tôi cách tư duy:

- **Product thinking:** Spec → Feedback → Iterate. Không ship v1, ship v4.
- **Security thinking:** Safety check trước mỗi trade, không phải sau khi bị hack.
- **Debug thinking:** Log everything, test từng step, không đoán mò.
- **Scope thinking:** 1 chain chạy thật > 4 chains chạy giả.
- **Honesty thinking:** Thừa nhận limitations = BGK tin tưởng hơn.

### Nếu không có AI:

Tôi sẽ cần:
- ~3-6 tháng học React/Next.js + Solana dev + API integration
- ~$5,000-10,000 thuê developer
- Hoặc đơn giản là **không thể build được product này**

Với AI: **5 ngày, $0 dev cost, 1 người non-tech → working trading agent on Solana mainnet** 🚀

---

*Built with ❤️ bằng Claude Chat (Opus 4.6) + Claude Code*
